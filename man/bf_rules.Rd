% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bf-rules.R
\name{bf_rules}
\alias{bf_rules}
\title{Evaluate the manuscript reporting rules from a bias attribution fraction}
\usage{
bf_rules(
  baf,
  ciLo = NA_real_,
  ciHi = NA_real_,
  cal_p = NA_real_,
  p_bias_dom = NA_real_,
  uncal_p = NA_real_,
  biasThreshold = 0.5,
  alpha = 0.05,
  r4Threshold = 0.15
)
}
\arguments{
\item{baf}{Numeric scalar. The bias attribution fraction.}

\item{ciLo, ciHi}{Numeric scalars. Limits of the \code{BAF} interval. \code{NA}
(default) leaves \code{R3} and \code{C2} undetermined.}

\item{cal_p}{Numeric scalar. Calibrated p-value. \code{NA} (default) leaves
\code{R1}-\code{R4} undetermined.}

\item{p_bias_dom}{Numeric scalar. Probability that the true regime is
bias-dominated. \code{NA} (default) leaves \code{R4} undetermined.}

\item{uncal_p}{Numeric scalar. Uncalibrated p-value. \code{NA} (default) leaves
\code{R0} undetermined.}

\item{biasThreshold}{BAF at or above which an estimate is bias-dominated
(default 0.5).}

\item{alpha}{Significance level for the p-value rules (default 0.05).}

\item{r4Threshold}{Upper limit of \code{P(BD)} for \code{R4} (default 0.15).}
}
\value{
A data frame with one row per rule and columns \code{rule}, \code{criterion}
and \code{pass} (logical, or \code{NA} when an input was missing).
}
\description{
Applies the seven pre-specified reporting rules evaluated in the companion
manuscript's performance table: the five nested rules \code{R0}-\code{R4} and the two
stand-alone comparators \code{C1}, \code{C2}. Each rule returns whether the estimate
would be declared usable as effect evidence under that rule.
}
\details{
The rules, in the manuscript's notation (\code{BAF} = bias attribution fraction,
\code{cal_p} = calibrated p-value from \code{\link[=ber_estimate]{ber_estimate()}}, \code{P(BD)} = probability
that the true regime is bias-dominated):

\describe{
\item{\code{R0}}{Uncalibrated p below \code{alpha}. No bias correction.}
\item{\code{R1}}{\code{cal_p} below \code{alpha} (Layer 1 only).}
\item{\code{R2}}{\code{R1} and the \code{BAF} point estimate below \code{biasThreshold}.}
\item{\code{R3}}{\code{R1} and the whole \code{BAF} interval at or below
\code{biasThreshold}.}
\item{\code{R4}}{\code{R1} and \code{P(BD)} below 0.15.}
\item{\code{C1}}{\code{BAF} point estimate below \code{biasThreshold}, without Layer 1.}
\item{\code{C2}}{The whole \code{BAF} interval at or below \code{biasThreshold},
without Layer 1.}
}

Note the deliberate asymmetry that separates \code{R2} from \code{R3}: \code{R2} tests the
point estimate with a strict inequality, \code{R3} tests the interval's upper
limit with a non-strict one. This mirrors the manuscript's definitions, so
an estimate with \code{BAF = 0.5} exactly fails \code{R2} and passes \code{R3}.

A rule is reported as \code{NA} when an input it needs was not supplied, rather
than as \code{FALSE}, so that a partially specified call cannot be misread as a
negative decision.
}
\examples{
# A genuine signal with a modest BAF whose interval stays below 0.5:
# R2 and R3 both pass, but the three-zone classifier calls it "mixed".
bf_rules(0.45, ciLo = 0.30, ciHi = 0.48, cal_p = 0.01, p_bias_dom = 0.12)
bf_classify(0.45, ciLo = 0.30, ciHi = 0.48)

# A point estimate exactly at the threshold passes R3 but not R2.
bf_rules(0.50, ciLo = 0.40, ciHi = 0.50, cal_p = 0.01)$pass
}
\seealso{
\code{\link[=bf_classify]{bf_classify()}} for the three-zone classification, which answers a
different question; \code{\link[=ber_screen]{ber_screen()}} for the two-layer screen that
implements \code{R1} and \code{R4} together with the verdict bands.
}
