% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-baf-gauge.R, R/zz-deprecated-aliases.R
\name{plot_baf_gauge}
\alias{plot_baf_gauge}
\alias{plot_bf_gauge}
\title{BAF gauge: point estimate and CI on the bounded 0-1 scale}
\usage{
plot_baf_gauge(x, digits = 2)

plot_bf_gauge(...)
}
\arguments{
\item{x}{An object of class \code{bafratio} (uses its bootstrap CI and median
point estimate) or \code{baf} (point estimate only).}

\item{digits}{Rounding for annotations.}

\item{...}{Arguments passed on to \code{\link[=plot_baf_gauge]{plot_baf_gauge()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
The signature plot of the v0.2.0 API. Shows the bias attribution fraction (BAF) and its
bootstrap CI on the bounded unit interval, divided into the three
credibility zones: effect-dominated (< 1/3), mixed (1/3-0.5), and
bias-dominated (> 0.5). BAF values near 1 should be read as signal
saturation by bias, not as precise fractions.
}
\examples{
fit <- baf_analyze(sim_est$logRr, sim_est$seLogRr,
                   sim_nc$logRr, sim_nc$seLogRr, nBoot = 500, seed = 42)
plot_baf_gauge(fit)
}
