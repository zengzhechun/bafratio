% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ber-classify.R, R/bsr-aliases.R
\name{ber_classify}
\alias{ber_classify}
\alias{bsr_classify}
\title{Classify an effect estimate from its BER}
\usage{
ber_classify(
  ber,
  ciLo = NA_real_,
  ciHi = NA_real_,
  biasThreshold = 1,
  effectThreshold = 0.5
)

bsr_classify(
  ber,
  ciLo = NA_real_,
  ciHi = NA_real_,
  biasThreshold = 1,
  effectThreshold = 0.5
)
}
\arguments{
\item{ber}{Numeric scalar. The bias-effect ratio.}

\item{ciLo}{Numeric scalar. Lower bootstrap confidence limit; \code{NA}
(default) triggers point-estimate classification.}

\item{ciHi}{Numeric scalar. Upper bootstrap confidence limit; \code{NA}
(default) triggers point-estimate classification.}

\item{biasThreshold}{BER above which an estimate is bias-dominated
(default 1: bias exceeds calibrated signal).}

\item{effectThreshold}{BER below which an estimate is effect-dominated
(default 0.5: calibrated signal is at least twice the bias).}
}
\value{
A character scalar: \code{"bias-dominated"}, \code{"competitive"},
\code{"effect-dominated"}, or \code{"unclassifiable"} (when \code{ber} is \code{NA}/\code{NaN}).
}
\description{
Three-zone classification of the credibility of an observational effect
estimate. When a bootstrap confidence interval is available, the
classification uses the interval (conservative); otherwise it falls back
to the point estimate.
}
\examples{
ber_classify(8.7)                        # 点估计：偏倚主导
ber_classify(8.7, ciLo = 2.9, ciHi = 35) # CI 下界 > 1：偏倚主导
ber_classify(0.03, ciLo = 0.01, ciHi = 0.1) # 效应主导
ber_classify(0.8, ciLo = 0.2, ciHi = 3)  # 跨阈值：势均力敌
}
