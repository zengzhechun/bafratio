% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ber-diagnostics.R, R/bsr-aliases.R
\name{ber_diagnostics}
\alias{ber_diagnostics}
\alias{bsr_diagnostics}
\title{Diagnostics for the empirical null fit}
\usage{
ber_diagnostics(ncLogRr, ncSeLogRr, ncNames = NULL)

bsr_diagnostics(ncLogRr, ncSeLogRr, ncNames = NULL)
}
\arguments{
\item{ncLogRr}{Numeric vector. Log effect estimates for the negative
control outcomes (or exposure controls); their true effects are assumed
to be null.}

\item{ncSeLogRr}{Numeric vector. Standard errors of \code{ncLogRr}.}

\item{ncNames}{Optional character vector of labels for the negative
controls, used in plots and diagnostics. Defaults to \code{names(ncLogRr)},
or \verb{NC1 ... NCK} when no names are available.}
}
\value{
An object of class \code{ber_diag}: a list with components
\describe{
\item{mu, sigma}{Fitted empirical null parameters.}
\item{residuals}{Raw residuals \code{ncLogRr - mu}.}
\item{std_residuals}{Residuals standardized by the marginal SD.}
\item{shapiro_p}{Shapiro-Wilk normality p-value for the standardized
residuals (\code{NA} when the test is not applicable).}
\item{max_resid_name, max_resid}{Label and value of the largest
absolute standardized residual — the prime outlier candidate.}
\item{qq}{Data frame with \code{theoretical} and \code{sample} quantiles for
Q-Q plotting.}
\item{nc_names}{Negative control labels.}
}
}
\description{
Assesses whether the negative control estimates are compatible with the
fitted empirical null distribution, using standardized residuals, a
Shapiro-Wilk normality test, and Q-Q data.
}
\details{
Under the empirical null, each NC estimate satisfies
\eqn{\hat\gamma_k \sim N(\mu_B, \tau_k^2 + \sigma_B^2)}, where
\eqn{\tau_k} is its sampling standard error. Standardized residuals are
therefore computed against the \strong{marginal} SD
\eqn{\sqrt{\tau_k^2 + \sigma_B^2}}, not against \eqn{\tau_k} alone:
the systematic-error variance is part of the total variance.
}
\examples{
diag <- ber_diagnostics(sim_nc$logRr, sim_nc$seLogRr,
                        ncNames = sim_nc$outcome)
diag
}
