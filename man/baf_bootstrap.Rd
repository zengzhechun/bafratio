% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baf-bootstrap.R, R/zz-deprecated-aliases.R
\name{baf_bootstrap}
\alias{baf_bootstrap}
\alias{ber_bootstrap}
\alias{bsr_bootstrap}
\title{Bootstrap confidence interval for the bias-effect ratio}
\usage{
baf_bootstrap(
  logRr,
  seLogRr,
  ncLogRr,
  ncSeLogRr,
  nBoot = 2000,
  seed = NULL,
  level = 0.95,
  method = c("log", "percentile")
)

ber_bootstrap(...)

bsr_bootstrap(...)
}
\arguments{
\item{logRr}{Numeric scalar. Log of the uncalibrated effect estimate
(e.g. log relative risk) for the exposure-outcome pair of interest.}

\item{seLogRr}{Numeric scalar. Standard error of \code{logRr}.}

\item{ncLogRr}{Numeric vector. Log effect estimates for the negative
control outcomes (or exposure controls); their true effects are assumed
to be null.}

\item{ncSeLogRr}{Numeric vector. Standard errors of \code{ncLogRr}.}

\item{nBoot}{Number of bootstrap replicates (default 2000).}

\item{seed}{Optional random seed for reproducibility. Defaults to \code{NULL},
in which case the caller's RNG is left untouched; when supplied, the
seed is localized with \code{\link[withr:with_seed]{withr::with_seed()}} and does \strong{not} pollute the
global RNG stream.}

\item{level}{Confidence level (default 0.95).}

\item{method}{\code{"log"} (default; percentile interval on the log scale, then
exponentiated) or \code{"percentile"} (raw percentile interval).}

\item{...}{Arguments passed on to \code{\link[=baf_bootstrap]{baf_bootstrap()}}.}
}
\value{
An object of class \code{baf_boot}: a list with components
\describe{
\item{ci_lo, ci_hi}{Confidence limits for the BER.}
\item{baf_ci_lo, baf_ci_hi}{Confidence limits for the BAF, obtained as
the monotone transform of the log-scale BER interval (equivalently,
a percentile interval on the logit scale).}
\item{level, method}{As supplied.}
\item{ber}{Raw-ratio point estimate of the BER (from
\code{\link[=baf_estimate]{baf_estimate()}}).}
\item{ber_median, baf_median}{Bootstrap-median point estimates
(median on the log scale, back-transformed). These are the primary
reported point estimates: they come from the same distribution as
the CI, so they cannot fall outside it.}
\item{boot_dist, boot_bf_dist}{Numeric vectors of length \code{nBoot}
with the bootstrap BER and BAF replicates (\code{NA} for failed
iterations).}
\item{n_fail, n_boot, success_rate}{Convergence diagnostics.}
}
}
\description{
Resamples the negative controls with replacement, refits the empirical
null for each replicate, and recomputes the BER. The confidence interval
is computed on the log scale and exponentiated back.
}
\details{
The BER is a strictly positive, right-skewed ratio estimator. Computing
the percentile interval on the log scale yields a roughly symmetric
bootstrap distribution and confidence limits with correct
order-of-magnitude coverage — standard practice for ratio-type estimators
such as risk ratios. The raw percentile interval is available as an
alternative via \code{method = "percentile"}.

Bootstrap replicates for which \code{\link[EmpiricalCalibration:fitNull]{EmpiricalCalibration::fitNull()}} fails to
converge are recorded as \code{NA} and excluded from the interval; a warning is
raised when the success rate drops below 50\%.
}
\examples{
# 演示用较少重抽样；正式分析建议 nBoot >= 2000
boot <- baf_bootstrap(sim_est$logRr, sim_est$seLogRr,
                      sim_nc$logRr, sim_nc$seLogRr,
                      nBoot = 500, seed = 42)
boot
}
