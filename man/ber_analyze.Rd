% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ber-analyze.R, R/bsr-aliases.R
\name{ber_analyze}
\alias{ber_analyze}
\alias{bsr_analyze}
\title{Full bias-effect ratio analysis in one call}
\usage{
ber_analyze(
  logRr,
  seLogRr,
  ncLogRr,
  ncSeLogRr,
  ncNames = NULL,
  nBoot = 2000,
  seed = NULL,
  level = 0.95,
  loo = FALSE,
  looBoot = 500,
  method = c("plugin", "robust")
)

bsr_analyze(
  logRr,
  seLogRr,
  ncLogRr,
  ncSeLogRr,
  ncNames = NULL,
  nBoot = 2000,
  seed = NULL,
  level = 0.95,
  loo = FALSE,
  looBoot = 500,
  method = c("plugin", "robust")
)
}
\arguments{
\item{logRr}{Numeric scalar. Log of the uncalibrated effect estimate
(e.g. log relative risk) for the exposure-outcome pair of interest.}

\item{seLogRr}{Numeric scalar. Standard error of \code{logRr}.}

\item{ncLogRr}{Numeric vector. Log effect estimates for the negative
control outcomes (or exposure controls); their true effects are assumed
to be null.}

\item{ncSeLogRr}{Numeric vector. Standard errors of \code{ncLogRr}.}

\item{ncNames}{Optional character vector of labels for the negative
controls, used in plots and diagnostics. Defaults to \code{names(ncLogRr)},
or \verb{NC1 ... NCK} when no names are available.}

\item{nBoot}{Number of bootstrap replicates (default 2000).}

\item{seed}{Optional random seed for reproducibility of the bootstrap
and leave-one-out resampling. Defaults to \code{NULL}, in which case no seed
is set and the caller's RNG is left untouched — passing a seed does
\strong{not} pollute the global RNG stream (it is localized with
\code{\link[withr:with_seed]{withr::with_seed()}}). Reproducibility is therefore the caller's
responsibility unless a seed is supplied.}

\item{level}{Confidence level (default 0.95).}

\item{loo}{Logical. Also run leave-one-out analysis? (default \code{FALSE})}

\item{looBoot}{Bootstrap replicates per leave-one-out iteration
(default 500; only used when \code{loo = TRUE}).}

\item{method}{Calibrated p-value method. \code{"plugin"} (default) uses the
Gaussian / null-normal calibrated p-value from
\code{\link[EmpiricalCalibration:calibrateP]{EmpiricalCalibration::calibrateP()}} — the OHDSI convention and the
historical default of this package. \code{"robust"} applies the small-sample
correction from the companion paper's eAppendix 13: it inflates the
plug-in variance by the estimation variance of the empirical-null mean,
\eqn{\widehat{\mathrm{Var}}(\hat\mu_B)=[\sum_k(\hat\tau_k^2+\hat\sigma_B^2)^{-1}]^{-1}},
and references a Student-t distribution with \eqn{K-2} degrees of
freedom. Use \code{"robust"} (or report it as a sensitivity) when the number
of negative controls is below ~20, where the plug-in p-value is
materially anti-conservative.}
}
\value{
An object of class \code{biasratio}: a list with components
\code{estimate} (class \code{ber}), \code{bootstrap} (class \code{ber_boot}),
\code{classification} (CI-based), \code{diagnostics} (class \code{ber_diag}),
\code{loo} (\code{NULL} or class \code{ber_loo}), and \code{call}.
}
\description{
Runs the complete workflow: BER point estimate, bootstrap confidence
interval, three-zone classification, empirical null diagnostics, and
(optionally) leave-one-out sensitivity analysis.
}
\examples{
fit <- ber_analyze(sim_est$logRr, sim_est$seLogRr,
                   sim_nc$logRr, sim_nc$seLogRr,
                   ncNames = sim_nc$outcome, nBoot = 500, seed = 42)
fit
}
