% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baf-loo.R, R/zz-deprecated-aliases.R
\name{baf_loo}
\alias{baf_loo}
\alias{ber_loo}
\alias{bsr_loo}
\title{Leave-one-out sensitivity analysis for the BER}
\usage{
baf_loo(
  logRr,
  seLogRr,
  ncLogRr,
  ncSeLogRr,
  ncNames = NULL,
  nBoot = 500,
  seed = NULL,
  method = c("plugin", "robust")
)

ber_loo(...)

bsr_loo(...)
}
\arguments{
\item{logRr}{Numeric scalar. Log of the uncalibrated effect estimate
(e.g. log relative risk) for the exposure-outcome pair of interest.}

\item{seLogRr}{Numeric scalar. Standard error of \code{logRr}.}

\item{ncLogRr}{Numeric vector. Log effect estimates for the negative
control outcomes (or exposure controls); their true effects are assumed
to be null.}

\item{ncSeLogRr}{Numeric vector. Standard errors of \code{ncLogRr}.}

\item{ncNames}{Optional character vector of labels for the negative
controls, used in plots and diagnostics. Defaults to \code{names(ncLogRr)},
or \verb{NC1 ... NCK} when no names are available.}

\item{nBoot}{Bootstrap replicates per exclusion (default 500). Set to 0
to skip confidence intervals (point estimates only).}

\item{seed}{Optional random seed. When supplied, iteration \code{i} uses
\code{seed + i}, so results are reproducible without stream coupling. The
seed is localized by \code{\link[=baf_bootstrap]{baf_bootstrap()}} and does not pollute the global
RNG stream.}

\item{method}{Calibrated p-value method. \code{"plugin"} (default) uses the
Gaussian / null-normal calibrated p-value from
\code{\link[EmpiricalCalibration:calibrateP]{EmpiricalCalibration::calibrateP()}} — the OHDSI convention and the
historical default of this package. \code{"robust"} applies the small-sample
correction from the companion paper's eAppendix 13: it inflates the
plug-in variance by the estimation variance of the empirical-null mean,
\eqn{\widehat{\mathrm{Var}}(\hat\mu_B)=[\sum_k(\hat\tau_k^2+\hat\sigma_B^2)^{-1}]^{-1}},
and references a Student-t distribution with \eqn{K-2} degrees of
freedom. Use \code{"robust"} (or report it as a sensitivity) when the number
of negative controls is below ~20, where the plug-in p-value is
materially anti-conservative.}

\item{...}{Arguments passed on to \code{\link[=baf_loo]{baf_loo()}}.}
}
\value{
A data frame of class \code{baf_loo} with one row per excluded
negative control: \code{excluded}, \code{ber}, \code{ci_lo}, \code{ci_hi}, \code{classification}.
}
\description{
Recomputes the BER (and optionally its bootstrap CI) after excluding each
negative control in turn. Identifies whether the conclusion depends on a
single influential control.
}
\examples{
loo <- baf_loo(sim_est$logRr, sim_est$seLogRr,
               sim_nc$logRr, sim_nc$seLogRr,
               ncNames = sim_nc$outcome, nBoot = 100, seed = 1)
loo
}
