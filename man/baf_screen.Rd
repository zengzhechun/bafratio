% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baf-screen.R, R/zz-deprecated-aliases.R
\name{baf_screen}
\alias{baf_screen}
\alias{bf_screen}
\alias{ber_screen}
\title{Two-layer screening of an observational effect estimate}
\usage{
baf_screen(
  logRr,
  seLogRr,
  ncLogRr,
  ncSeLogRr,
  ncNames = NULL,
  nBoot = 2000,
  seed = NULL,
  level = 0.95,
  method = c("plugin", "robust"),
  reliability = baf_reliability,
  cal_p_threshold = 0.05,
  ...
)

bf_screen(...)

ber_screen(...)

bf_screen(...)
}
\arguments{
\item{logRr}{Either a \code{bafratio} object (from \code{\link[=baf_analyze]{baf_analyze()}}, which
already contains the estimate and bootstrap) or, in the default method,
the numeric log of the uncalibrated effect estimate (the primary
exposure-outcome pair). When a \code{bafratio} object is supplied, the
remaining arguments are ignored.}

\item{seLogRr}{Numeric scalar. Standard error of \code{logRr} (default method).}

\item{ncLogRr}{Numeric vector. Log effect estimates of the negative
controls (default method).}

\item{ncSeLogRr}{Numeric vector. Standard errors of \code{ncLogRr} (default
method).}

\item{ncNames}{Optional character vector of negative-control labels.}

\item{nBoot}{Bootstrap replicates for the BAF 95\% CI (default method only;
ignored when a \code{bafratio} object is supplied, since it already has the
bootstrap).}

\item{seed}{Optional random seed for the bootstrap (default method).}

\item{level}{Confidence level for the BAF CI (default 0.95).}

\item{method}{Calibrated p method, \code{"plugin"} (default) or \code{"robust"}
(passed to \code{\link[=baf_estimate]{baf_estimate()}} in the default method).}

\item{reliability}{Reference calibration table (default \link{baf_reliability}).
A data frame with columns \code{baf_center}, \code{p_bias_dom_narrow},
\code{p_bias_dom_wide}; the median CI half-width is read from its
\code{med_ci_width} attribute (default 0.133).}

\item{cal_p_threshold}{Layer-1 threshold for the calibrated p-value
(default 0.05).}

\item{...}{Ignored.}
}
\value{
An object of class \code{baf_screen}: a list with
\describe{
\item{layer1}{list with \code{cal_p}, \code{threshold}, \code{passed}.}
\item{layer2}{list with \code{baf}, \code{baf_ci_lo}, \code{baf_ci_hi},
\code{ci_half_width}, \code{ci_class} (\code{"narrow"}/\code{"wide"}), \code{clipped}
(logical, TRUE when the CI hit the 0/1 boundary), \code{bin} (the matched
row of \code{reliability}), \code{p_bias_dom_narrow}, \code{p_bias_dom_wide}, and
\code{p_bias_dom} (the conservative maximum).}
\item{verdict}{list with \code{code}, \code{label}, \code{explanation} (English).}
\item{reliability_source}{Provenance of the calibration table used.}
}
}
\description{
Decides whether an observational effect estimate can serve as \emph{effect
evidence}, using a two-layer procedure:
}
\details{
\enumerate{
\item \strong{Layer 1 (signal existence).} The calibrated p-value (\code{cal_p}) from
\code{\link[=baf_estimate]{baf_estimate()}} is compared with \code{cal_p_threshold} (default 0.05). A
calibrated p at or above the threshold means no signal distinct from
chance is established, so the bias screening in Layer 2 is not entered
and the verdict is \code{"insufficient-evidence"}.
\item \strong{Layer 2 (bias share).} Conditional on a real signal, the bias attribution fraction
\code{BAF} and its 95\% CI are used to look up, in the reference simulation
calibration \link{baf_reliability}, the probability that the \emph{true} regime is
bias-dominated. Because a wide CI implies a poorly pinned-down BAF, both
the narrow-CI and wide-CI conditional probabilities are read and the
conservative maximum is reported as \code{P(bias-dominated)}. The verdict then
follows fixed bands:
\verb{< 0.15} effect-evidence, \code{0.15-0.45} mixed / hypothesis-generating,
\code{0.45-0.65} competitive / inconclusive, \verb{>= 0.65} not-effect-evidence.
}

The function returns a \code{baf_screen} object whose \code{print}/\code{summary} show the
numeric decision and whose \code{explain()} method returns a plain-English
interpretation. When the BAF 95\% CI is truncated at the 0 or 1 boundary, the
usable one-sided half-interval is used for the width classification.

\strong{Interval coverage caveat.} The \code{BAF} interval underlying both layers is
an approximate (bootstrap / posterior) interval whose empirical coverage in
the reference simulation is below the nominal 95\% (71.1\% overall, 58.5\%
narrow, 83.8\% wide; companion manuscript, v39). The verdict bands therefore
carry more uncertainty than a nominal 95\% interval implies. See the
\link{baf_reliability} reference table this screening reads.

The two-layer screen implements rules \code{R1} and \code{R4} of the companion
manuscript's rule table. The remaining rules, including the interval-based
\code{R3}/\code{C2}, are available from \code{\link[=baf_rules]{baf_rules()}}.
}
\examples{
# raw inputs
sc <- baf_screen(sim_est$logRr, sim_est$seLogRr,
                 sim_nc$logRr, sim_nc$seLogRr, nBoot = 500, seed = 42)
sc
explain(sc)

# or screen an existing baf_analyze() fit
fit <- baf_analyze(sim_est$logRr, sim_est$seLogRr,
                   sim_nc$logRr, sim_nc$seLogRr, nBoot = 500, seed = 42)
baf_screen(fit)
}
\seealso{
\code{\link[=baf_analyze]{baf_analyze()}} for the full workflow, \code{\link[=ber_classify]{ber_classify()}} /
\code{\link[=baf_classify]{baf_classify()}} for the simpler three-zone (BAF-only) classification,
\code{\link[=baf_rules]{baf_rules()}} for the full manuscript rule table, and \code{\link[=explain]{explain()}} for the
plain-English interpretation.
}
